{
  "$schema": "https://json-schema.org/draft-07/schema",
  "$id": "https://cruster.dev/schemas/get-secret/v1.json",
  "title": "cruster get secret (NDJSON record)",
  "description": "One record per secret. Default mode emits pruned k8s objects.",
  "type": "object",
  "required": ["metadata"]
}
