{
  "$schema": "https://json-schema.org/draft-07/schema",
  "$id": "https://cruster.dev/schemas/get-event/v1.json",
  "title": "cruster get event (NDJSON record)",
  "description": "One record per event. Default mode emits pruned k8s objects.",
  "type": "object",
  "required": ["metadata"]
}
