{
  "$schema": "https://json-schema.org/draft-07/schema",
  "$id": "https://cruster.dev/schemas/get-daemonset/v1.json",
  "title": "cruster get daemonset (NDJSON record)",
  "description": "One record per daemonset. Default mode emits pruned k8s objects.",
  "type": "object",
  "required": ["metadata"]
}
