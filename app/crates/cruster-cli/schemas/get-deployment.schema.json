{
  "$schema": "https://json-schema.org/draft-07/schema",
  "$id": "https://cruster.dev/schemas/get-deployment/v1.json",
  "title": "cruster get deployment (NDJSON record)",
  "description": "One record per deployment. Default mode emits pruned k8s objects.",
  "type": "object",
  "required": ["metadata"]
}
